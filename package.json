
{
  "name": "gulp-encrypt",
  "version": "0.2.0",
  "description": "A simple gulp plugin to encrypt files",
  "keywords": [
    "gulpplugin",
    "encrypt"
  ],
  "author": {
    "name": "charliedowler",    
    "url": "https://github.com/charliedowler"
  },
  "repository": {
    "url": "git://github.com/charliedowler/gulp-encrypt",
    "job": "https://api.travis-ci.org/repositories/charliedowler/gulp-encrypt"
  },
  "scripts": {
    "test": "istanbul test _mocha --report html -- test/*.js --reporter spec",
    "coveralls": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "dependencies": {
    "through2": "^0.6.1",
    "gulp-util": "^3.0.1"
  },
  "devDependencies": {
    "mocha": "^1.21.4",
    "mocha-lcov-reporter": "^0.0.1",
    "istanbul": "^0.3.2",
    "event-stream": "^3.1.7",
    "should": "^4.0.4"
  },
  "engines": {
    "node": ">=0.8.0",
    "npm": ">=1.2.10"
  },
  "license": "MIT"
}
